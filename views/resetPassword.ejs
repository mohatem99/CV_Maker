<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>Password Reset</title>
    <meta name="description" content="" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="/css/login.css" />
    <style>
      nav {
        position: absolute;
        top: 80px;
        right: 0;
      }
      #navbar {
        width: 95%;
      }
    </style>
  </head>

  <body>
    <div id="container">
      <nav id="navbar">
        <a href="index.html">
          <img
            src="/images/arrow.svg"
            alt="menu icon"
            id="menu_icon"
            width="40px"
            height="25px"
        /></a>
      </nav>
      <div id="form_container">
        <h1>Password Reset</h1>
        <br /><br />
        <form action="/handle-reset-password" method="POST">
          <label for="password">
            <strong>Enter password</strong><br />
            <input
              type="password"
              name="password"
              class="btn"
              id="password"
              minlength="8"
              placeholder="  ******"
              required
            />
          </label>
          <label for="password-confirmation">
            <strong>confirm your password</strong><br />
            <input
              type="password"
              name="confirmpassword"
              class="btn"
              minlength="8"
              id="password-confirm"
              placeholder="  ******"
              required
            />
          </label>
          <br />
          <input type="submit" value="Confirm" id="submit_button" />
        </form>
      </div>
    </div>

    <script>
      //   let submitBtn = document.getElementById("submit_button");
      //   let matchIdentifier = document.getElementById("match");
      //   let passVal = document.getElementById("password");
      //   let passConfirm = document.getElementById("password-confirm");

      //   passVal.addEventListener("keyup", () => {
      //     let passVal = document.getElementById("password");
      //     let passConfirm = document.getElementById("password-confirm");
      //     if (passVal.value === passConfirm.value) {
      //       passVal.style.border = "rgb(39, 240, 106) solid 2px";
      //       passConfirm.style.border = "rgb(39, 240, 106) solid 2px";
      //     } else {
      //       passVal.style.border = "red solid 2px";
      //       passConfirm.style.border = "red solid 2px";
      //     }
      //   });
      //   passConfirm.addEventListener("keyup", () => {
      //     let passVal = document.getElementById("password");
      //     let passConfirm = document.getElementById("password-confirm");
      //     if (passVal.value === passConfirm.value) {
      //       passVal.style.border = "rgb(39, 240, 106) solid 2px";
      //       passConfirm.style.border = "rgb(39, 240, 106) solid 2px";
      //     } else {
      //       passVal.style.border = "red solid 2px";
      //       passConfirm.style.border = "red solid 2px";
      //     }
      //   });

      //   submitBtn.addEventListener("click", (event) => {
      //     let passVal = document.getElementById("password");
      //     let passConfirm = document.getElementById("password-confirm");
      //     if (passVal.value !== passConfirm.value) {
      //       event.preventDefault();
      //       alert("passwords does not match");
      //     }
      //   });
    </script>
  </body>
</html>
